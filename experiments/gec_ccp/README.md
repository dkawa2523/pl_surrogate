# GEC-CCP Experiment Archive

One-off GEC-CCP plotting and analysis utilities live here so that mainline
scripts stay limited to reusable entry points.

Run archived utilities from the repository root when reproducing historical
reports. Their outputs under `reports/` and `runs/` are generated artifacts.
